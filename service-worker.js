const CACHE='offline-scanner-poc-v1';
const APP_SHELL=['./','./index.html','./manifest.webmanifest'];
const SCANNER='https://cdn.jsdelivr.net/npm/html5-qrcode@2.3.8/html5-qrcode.min.js';
self.addEventListener('install',event=>{event.waitUntil((async()=>{const cache=await caches.open(CACHE);await cache.addAll(APP_SHELL);try{await cache.add(SCANNER)}catch(error){console.warn('Scanner library was not cached during install',error)}await self.skipWaiting()})())});
self.addEventListener('activate',event=>{event.waitUntil((async()=>{for(const key of await caches.keys())if(key!==CACHE)await caches.delete(key);await self.clients.claim()})())});
self.addEventListener('fetch',event=>{if(event.request.method!=='GET')return;const url=new URL(event.request.url);if(event.request.mode==='navigate'){event.respondWith((async()=>{const cached=await caches.match('./index.html');try{const fresh=await fetch(event.request);const cache=await caches.open(CACHE);cache.put('./index.html',fresh.clone());return fresh}catch{return cached||Response.error()}})());return}if(url.href===SCANNER){event.respondWith((async()=>{const cached=await caches.match(SCANNER);if(cached)return cached;const response=await fetch(event.request);const cache=await caches.open(CACHE);cache.put(SCANNER,response.clone());return response})());return}event.respondWith((async()=>{const cached=await caches.match(event.request);if(cached)return cached;try{return await fetch(event.request)}catch{return new Response('',{status:504,statusText:'Offline'})}})())});